<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kayhan.automation">

    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Home_Activity"
            android:label="@string/app_name"
            android:screenOrientation="landscape" />
        <activity
            android:name=".Search_Activity"
            android:label="@string/title_activity_search_"
            android:screenOrientation="landscape"/>
        <activity
            android:name=".Room_Activity"
            android:label="@string/title_activity_room_"
            android:screenOrientation="landscape">

        </activity>

        <!-- <receiver android:name=".BluetoothReceiver"> -->
        <!-- <intent-filter > -->
        <!-- <action android:name="ANDROID.INTENT.ACTION.BOOT_COMPLETED"/> -->
        <!-- </intent-filter> -->
        <!-- </receiver> -->
        <activity
            android:name=".Splash"
            android:label="@string/title_activity_splash"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HomeNavigation"
            android:label="@string/title_activity_home_navigation"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="landscape"/>
        <activity
            android:name=".GraphChart"
            android:label="@string/title_activity_graph_chart"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="landscape">

        </activity>
    </application>

</manifest>
